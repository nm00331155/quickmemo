プロンプト1: 電卓の修正
以下をCopilotに渡してください。

Copy# 指示: 電卓BottomSheetの修正

## 変更点一覧
1. 1行目ボタン `[CE][(][)][÷]` → `[×マーク][税込][税抜][÷]` に変更
2. 税率はDataStoreで設定可能にする（デフォルト10%）
3. 式+結果の挿入を改行ではなく1行で表示（例: `3,600×155 =558,000`）
4. 連続挿入時は先頭に改行を1つ入れる（くっつき防止）
5. 電卓を閉じても履歴が消えないようにする（ViewModel経由で保持）

## 修正ファイル

### 1. `CalculatorBottomSheet.kt` — 修正

#### A. 引数に税率と履歴を追加

関数シグネチャを変更:

```kotlin
@OptIn(ExperimentalMaterial3Api::class)
@Composable
fun CalculatorBottomSheet(
    cursorContextText: String,
    taxRate: Double,
    history: MutableList<CalcHistoryEntry>,
    onInsertExpressionAndResult: (expression: String, result: String) -> Unit,
    onInsertResultOnly: (result: String) -> Unit,
    onDismiss: () -> Unit,
) {
```

#### B. history をローカル変数から引数に変更

**削除**:
```kotlin
val history = remember { mutableStateListOf<CalcHistoryEntry>() }
```

引数の `history` をそのまま使用する。`mutableStateListOf` ではなく `SnapshotStateList` を外から渡す形になるので、呼び出し元で管理する。

#### C. 税込/税抜の計算関数を追加

`clearAll()` の後に追加:

```kotlin
fun applyTaxIncluded() {
    // 現在の結果に税込計算を適用
    if (displayResult == "0" || displayResult == "エラー") return
    try {
        val current = BigDecimal(displayResult.replace(",", ""))
        val taxMultiplier = BigDecimal.ONE + BigDecimal.valueOf(taxRate / 100.0)
        val taxIncluded = current.multiply(taxMultiplier)
            .setScale(0, java.math.RoundingMode.HALF_UP)
        val formatted = formatWithCommas(taxIncluded.toPlainString())
        val taxExpr = "$displayResult×${taxRate.let { if (it == it.toLong().toDouble()) it.toLong().toString() else it.toString() }}%税込"
        expression = taxExpr
        displayResult = formatted
        hasError = false
        history.add(CalcHistoryEntry(expression = taxExpr, result = formatted))
    } catch (_: Exception) {
        displayResult = "エラー"
        hasError = true
    }
}

fun applyTaxExcluded() {
    // 現在の結果から税抜計算を適用
    if (displayResult == "0" || displayResult == "エラー") return
    try {
        val current = BigDecimal(displayResult.replace(",", ""))
        val taxMultiplier = BigDecimal.ONE + BigDecimal.valueOf(taxRate / 100.0)
        val taxExcluded = current.divide(taxMultiplier, 0, java.math.RoundingMode.HALF_UP)
        val formatted = formatWithCommas(taxExcluded.toPlainString())
        val taxExpr = "$displayResult÷${taxRate.let { if (it == it.toLong().toDouble()) it.toLong().toString() else it.toString() }}%税抜"
        expression = taxExpr
        displayResult = formatted
        hasError = false
        history.add(CalcHistoryEntry(expression = taxExpr, result = formatted))
    } catch (_: Exception) {
        displayResult = "エラー"
        hasError = true
    }
}
```

#### D. 1行目のボタン定義を変更

`buttonRows` の最初の `listOf` を変更:

**変更前**:
```kotlin
listOf(
    CalcButton("CE", CalcButtonType.FUNCTION),
    CalcButton("(", CalcButtonType.OPERATOR),
    CalcButton(")", CalcButtonType.OPERATOR),
    CalcButton("÷", CalcButtonType.OPERATOR),
),
```

**変更後**:
```kotlin
listOf(
    CalcButton("✕", CalcButtonType.FUNCTION),
    CalcButton("税込", CalcButtonType.TAX),
    CalcButton("税抜", CalcButtonType.TAX),
    CalcButton("÷", CalcButtonType.OPERATOR),
),
```

#### E. CalcButtonType に TAX を追加

```kotlin
private enum class CalcButtonType {
    NUMBER,
    OPERATOR,
    FUNCTION,
    EQUALS,
    TAX,
}
```

#### F. CalcKeyButton の色に TAX を追加

```kotlin
val bgColor = when (button.type) {
    CalcButtonType.NUMBER -> MaterialTheme.colorScheme.surfaceContainerHigh
    CalcButtonType.OPERATOR -> Color(0xFF26808F)
    CalcButtonType.FUNCTION -> Color(0xFFE53935)  // ×マーク: 赤系
    CalcButtonType.EQUALS -> Color(0xFF1565C0)
    CalcButtonType.TAX -> Color(0xFF7B1FA2)  // 税: 紫系
}
val textColor = when (button.type) {
    CalcButtonType.NUMBER -> MaterialTheme.colorScheme.onSurface
    else -> Color.White
}
```

#### G. ボタンのクリックハンドラを変更

`buttonRows.forEach` 内の `when` を変更:

**変更前**:
```kotlin
when (button.label) {
    "CE" -> backspace()
    "=" -> evaluate()
    "−" -> appendToExpression("-")
    else -> appendToExpression(button.label)
}
```

**変更後**:
```kotlin
when (button.label) {
    "✕" -> backspace()
    "=" -> evaluate()
    "−" -> appendToExpression("-")
    "税込" -> applyTaxIncluded()
    "税抜" -> applyTaxExcluded()
    else -> appendToExpression(button.label)
}
```

長押しのハンドラも変更:

**変更前**:
```kotlin
onLongClick = {
    if (button.label == "CE") {
        clearAll()
    }
},
```

**変更後**:
```kotlin
onLongClick = {
    if (button.label == "✕") {
        clearAll()
    }
},
```

#### H. 税率の表示（履歴エリアの上に小さく表示）

`cursorContextText` の表示ブロックの直後に追加:

```kotlin
Text(
    text = "税率: ${if (taxRate == taxRate.toLong().toDouble()) "${taxRate.toLong()}%" else "$taxRate%"}",
    style = MaterialTheme.typography.labelSmall,
    color = MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = 0.6f),
    modifier = Modifier.padding(horizontal = 4.dp, vertical = 2.dp),
)
```

### 2. `EditorScreen.kt` — 挿入ロジックの修正

#### A. 挿入時の書式変更

`if (showCalculatorSheet)` ブロックの `onInsertExpressionAndResult` コールバック内を変更:

**変更前**:
```kotlin
onInsertExpressionAndResult = { expr, result ->
    appendTextToEditor("$expr\n=$result")
    showCalculatorSheet = false
},
```

**変更後**:
```kotlin
onInsertExpressionAndResult = { expr, result ->
    val prefix = if (lastCalcInsertedFlag) "\n" else ""
    appendTextToEditor("$prefix$expr =$result")
    lastCalcInsertedFlag = true
    showCalculatorSheet = false
},
```

`onInsertResultOnly` も同様:

**変更前**:
```kotlin
onInsertResultOnly = { result ->
    appendTextToEditor("=$result")
    showCalculatorSheet = false
},
```

**変更後**:
```kotlin
onInsertResultOnly = { result ->
    val prefix = if (lastCalcInsertedFlag) "\n" else ""
    appendTextToEditor("$prefix=$result")
    lastCalcInsertedFlag = true
    showCalculatorSheet = false
},
```

#### B. 連続挿入フラグの追加

変数宣言部に追加:

```kotlin
var lastCalcInsertedFlag by remember { mutableStateOf(false) }
```

メモを保存/ロードするタイミング（`LaunchedEffect(uiState.memoId, ...)` 等）でリセット:

```kotlin
lastCalcInsertedFlag = false
```

#### C. 電卓の履歴をViewModel経由で保持

変数宣言部に追加:

```kotlin
val calcHistory = remember { mutableStateListOf<CalcHistoryEntry>() }
```

**注意**: `remember` で保持するのでEditorScreenがコンポジションから外れない限り消えない。電卓の開閉では消えなくなる。もしメモ間を跨いでも残したい場合は `EditorViewModel` に `val calcHistory = mutableStateListOf<CalcHistoryEntry>()` を持たせる（推奨）。

#### D. CalculatorBottomSheet 呼び出しに引数追加

**変更前**:
```kotlin
CalculatorBottomSheet(
    cursorContextText = buildCursorContextText(activeRichTextState()),
    onInsertExpressionAndResult = { ... },
    onInsertResultOnly = { ... },
    onDismiss = { showCalculatorSheet = false },
)
```

**変更後**:
```kotlin
CalculatorBottomSheet(
    cursorContextText = buildCursorContextText(activeRichTextState()),
    taxRate = uiState.taxRate,
    history = calcHistory,
    onInsertExpressionAndResult = { expr, result ->
        val prefix = if (lastCalcInsertedFlag) "\n" else ""
        appendTextToEditor("$prefix$expr =$result")
        lastCalcInsertedFlag = true
        showCalculatorSheet = false
    },
    onInsertResultOnly = { result ->
        val prefix = if (lastCalcInsertedFlag) "\n" else ""
        appendTextToEditor("$prefix=$result")
        lastCalcInsertedFlag = true
        showCalculatorSheet = false
    },
    onDismiss = { showCalculatorSheet = false },
)
```

### 3. `EditorUiState.kt` — taxRate追加

```kotlin
data class EditorUiState(
    // ... 既存フィールド ...
    val taxRate: Double = 10.0,
)
```

### 4. `SettingsDataStore.kt` — 税率キーを追加

```kotlin
private val TAX_RATE_KEY = doublePreferencesKey("calculator_tax_rate")

val taxRateFlow: Flow<Double> = dataStore.data.map { prefs ->
    prefs[TAX_RATE_KEY] ?: 10.0
}

suspend fun setTaxRate(rate: Double) {
    dataStore.edit { prefs ->
        prefs[TAX_RATE_KEY] = rate
    }
}
```

### 5. `EditorViewModel.kt` — taxRate を UiState に反映

`init` ブロックまたは設定の collect 箇所に追加:

```kotlin
viewModelScope.launch {
    settingsRepository.observeTaxRate().collect { rate ->
        _uiState.update { it.copy(taxRate = rate) }
    }
}
```

`SettingsRepository` インターフェースと実装にも `observeTaxRate(): Flow<Double>` を追加。

### 6. 設定画面（メモタブ）に税率設定を追加

設定画面のメモタブに以下を追加:

- ラベル: 「電卓の税率」
- 入力: `OutlinedTextField` (数値, `KeyboardType.Decimal`)
- デフォルト値表示: 「10%」
- 保存: 入力確定時に `settingsDataStore.setTaxRate(value)` を呼ぶ
- バリデーション: 0〜100の範囲

実装の詳細は設定画面の既存パターンに合わせること。
Copy
プロンプト2: ロック画面通知の修正
以下をCopilotに渡してください。

Copy# 指示: ロック画面通知の改善 — 3行表示 + ロック解除なしでTodo/メモ追加

## 変更点一覧
1. 折りたたみ通知を3行表示にする（Todo上位3件 or メモプレビュー3行）
2. ロック解除せずにTodoやメモを追加できる Direct Reply 機能を追加
3. 展開時も3行以上のTodoを見やすくする

## 修正ファイル

### 1. `notification_collapsed.xml` — 3行表示に変更

**全置換**:

```xml
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp">

    <TextView
        android:id="@+id/notification_summary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@android:color/primary_text_light"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/notification_line1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@android:color/primary_text_light"
        android:maxLines="1"
        android:ellipsize="end"
        android:paddingTop="2dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/notification_line2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@android:color/primary_text_light"
        android:maxLines="1"
        android:ellipsize="end"
        android:paddingTop="1dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/notification_line3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="@android:color/secondary_text_light"
        android:maxLines="1"
        android:ellipsize="end"
        android:paddingTop="1dp"
        android:visibility="gone" />

</LinearLayout>
```

### 2. `QuickMemoForegroundService.kt` — Direct Reply 追加 + 3行表示

以下の変更を `buildNotification()` メソッドに適用する。

#### A. 折りたたみ通知の3行表示

`buildNotification()` 内の折りたたみ部分を変更:

**変更前の collapsedView 構築部分を全て削除し、以下で置換**:

```kotlin
// --- 折りたたみ表示 (3行) ---
val collapsedView = RemoteViews(packageName, R.layout.notification_collapsed)

val summaryText = if (totalCount > 0) {
    "☑ Todo $checkedCount/$totalCount 完了"
} else if (pinnedMemo != null) {
    "📝 ${pinnedMemo.title.ifBlank { "メモ" }}"
} else {
    "QuickMemo"
}
collapsedView.setTextViewText(R.id.notification_summary, summaryText)

// 3行にTodo or メモ内容を表示
val lineIds = listOf(R.id.notification_line1, R.id.notification_line2, R.id.notification_line3)

if (uncheckedItems.isNotEmpty()) {
    // Todoモード: 未完了3件を表示
    val displayItems = uncheckedItems.take(3)
    for (i in lineIds.indices) {
        if (i < displayItems.size) {
            collapsedView.setTextViewText(lineIds[i], "☐ ${displayItems[i].text}")
            collapsedView.setViewVisibility(lineIds[i], android.view.View.VISIBLE)
        } else {
            collapsedView.setViewVisibility(lineIds[i], android.view.View.GONE)
        }
    }
} else if (pinnedMemo != null) {
    // メモモード: ピン留めメモの内容を3行で表示
    val memoLines = pinnedMemo.contentPlainText
        .split("\n")
        .filter { it.isNotBlank() }
        .take(3)
    for (i in lineIds.indices) {
        if (i < memoLines.size) {
            collapsedView.setTextViewText(lineIds[i], memoLines[i].take(50))
            collapsedView.setViewVisibility(lineIds[i], android.view.View.VISIBLE)
        } else {
            collapsedView.setViewVisibility(lineIds[i], android.view.View.GONE)
        }
    }
} else {
    // データなし
    collapsedView.setTextViewText(lineIds[0], "タップして開く")
    collapsedView.setViewVisibility(lineIds[0], android.view.View.VISIBLE)
    collapsedView.setViewVisibility(lineIds[1], android.view.View.GONE)
    collapsedView.setViewVisibility(lineIds[2], android.view.View.GONE)
}
```

#### B. Direct Reply (ロック解除なしで入力) の追加

`buildNotification()` の通知構築部分（`return NotificationCompat.Builder(...)` の前）に以下を追加:

```kotlin
// --- Direct Reply: ロック画面からTodo追加 ---
val addTodoReplyKey = "key_add_todo_text"
val remoteInputTodo = androidx.core.app.RemoteInput.Builder(addTodoReplyKey)
    .setLabel("Todoを入力...")
    .build()

val addTodoBroadcastIntent = Intent(this, NotificationActionReceiver::class.java).apply {
    action = NotificationActionReceiver.ACTION_ADD_TODO
}
val addTodoPending = PendingIntent.getBroadcast(
    this,
    200,
    addTodoBroadcastIntent,
    PendingIntent.FLAG_MUTABLE or PendingIntent.FLAG_UPDATE_CURRENT,
)

val addTodoAction = NotificationCompat.Action.Builder(
    0,
    "Todo追加",
    addTodoPending,
).addRemoteInput(remoteInputTodo)
 .setAllowGeneratedReplies(false)
 .build()

// --- Direct Reply: ロック画面からメモ追加 ---
val addMemoReplyKey = "key_add_memo_text"
val remoteInputMemo = androidx.core.app.RemoteInput.Builder(addMemoReplyKey)
    .setLabel("メモを入力...")
    .build()

val addMemoBroadcastIntent = Intent(this, NotificationActionReceiver::class.java).apply {
    action = NotificationActionReceiver.ACTION_ADD_MEMO
}
val addMemoPending = PendingIntent.getBroadcast(
    this,
    201,
    addMemoBroadcastIntent,
    PendingIntent.FLAG_MUTABLE or PendingIntent.FLAG_UPDATE_CURRENT,
)

val addMemoAction = NotificationCompat.Action.Builder(
    0,
    "メモ追加",
    addMemoPending,
).addRemoteInput(remoteInputMemo)
 .setAllowGeneratedReplies(false)
 .build()
```

**重要**: `PendingIntent.FLAG_MUTABLE` が必須。`RemoteInput` は `FLAG_IMMUTABLE` だと入力テキストを受け取れない。

#### C. 通知ビルダーの `.addAction()` を変更

**変更前**:
```kotlin
.addAction(0, getString(R.string.quick_new_memo), newMemoPending)
.addAction(0, getString(R.string.quick_new_todo), newTodoPending)
```

**変更後**:
```kotlin
.addAction(addTodoAction)
.addAction(addMemoAction)
```

### 3. 新規ファイル: `app/src/main/java/com/quickmemo/app/receiver/NotificationActionReceiver.kt`

```kotlin
package com.quickmemo.app.receiver

import android.app.NotificationManager
import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent
import android.util.Log
import androidx.core.app.RemoteInput
import com.quickmemo.app.data.local.database.QuickMemoDatabase
import com.quickmemo.app.data.local.entity.MemoEntity
import com.quickmemo.app.data.local.entity.TodoItemEntity
import com.quickmemo.app.service.QuickMemoForegroundService
import com.quickmemo.app.widget.WidgetUpdateDispatcher
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.launch
import java.util.UUID

class NotificationActionReceiver : BroadcastReceiver() {

    override fun onReceive(context: Context, intent: Intent) {
        when (intent.action) {
            ACTION_ADD_TODO -> handleAddTodo(context, intent)
            ACTION_ADD_MEMO -> handleAddMemo(context, intent)
        }
    }

    private fun handleAddTodo(context: Context, intent: Intent) {
        val remoteInput = RemoteInput.getResultsFromIntent(intent) ?: return
        val inputText = remoteInput.getCharSequence("key_add_todo_text")?.toString()?.trim()
        if (inputText.isNullOrBlank()) return

        Log.d(TAG, "Adding Todo from lock screen: $inputText")

        val pendingResult = goAsync()
        CoroutineScope(Dispatchers.IO).launch {
            try {
                val db = QuickMemoDatabase.getInstance(context)
                val todoDao = db.todoDao()

                // タブ0（最初のタブ）の末尾に追加
                val maxOrder = todoDao.getMaxSortOrder(0) ?: -1
                val newItem = TodoItemEntity(
                    id = UUID.randomUUID().toString(),
                    tabId = 0,
                    text = inputText,
                    checked = 0,
                    dueDate = null,
                    sortOrder = maxOrder + 1,
                    createdAt = System.currentTimeMillis(),
                    checkedAt = null,
                )
                todoDao.insert(newItem)

                Log.d(TAG, "Todo added successfully: $inputText")

                // 通知とウィジェットを更新
                QuickMemoForegroundService.refreshFromOutside(context)
                WidgetUpdateDispatcher.updateTodoWidgets(context)
            } catch (e: Exception) {
                Log.e(TAG, "Failed to add todo", e)
            } finally {
                pendingResult.finish()
            }
        }
    }

    private fun handleAddMemo(context: Context, intent: Intent) {
        val remoteInput = RemoteInput.getResultsFromIntent(intent) ?: return
        val inputText = remoteInput.getCharSequence("key_add_memo_text")?.toString()?.trim()
        if (inputText.isNullOrBlank()) return

        Log.d(TAG, "Adding Memo from lock screen: $inputText")

        val pendingResult = goAsync()
        CoroutineScope(Dispatchers.IO).launch {
            try {
                val db = QuickMemoDatabase.getInstance(context)
                val memoDao = db.memoDao()

                val newMemo = MemoEntity(
                    title = "",
                    contentHtml = "<p>$inputText</p>",
                    contentPlainText = inputText,
                    colorLabel = 0,
                    isPinned = 0,
                    isLocked = 0,
                    isChecklist = 0,
                    isDeleted = 0,
                    createdAt = System.currentTimeMillis(),
                    updatedAt = System.currentTimeMillis(),
                    deletedAt = null,
                )
                memoDao.insert(newMemo)

                Log.d(TAG, "Memo added successfully: $inputText")

                // 通知を更新
                QuickMemoForegroundService.refreshFromOutside(context)
            } catch (e: Exception) {
                Log.e(TAG, "Failed to add memo", e)
            } finally {
                pendingResult.finish()
            }
        }
    }

    companion object {
        const val ACTION_ADD_TODO = "com.quickmemo.app.ACTION_ADD_TODO_FROM_NOTIFICATION"
        const val ACTION_ADD_MEMO = "com.quickmemo.app.ACTION_ADD_MEMO_FROM_NOTIFICATION"
        private const val TAG = "QM_NOTIF_ACTION"
    }
}
```

### 4. `AndroidManifest.xml` — BroadcastReceiver を登録

`<application>` タグ内に追加:

```xml
<receiver
    android:name=".receiver.NotificationActionReceiver"
    android:exported="false">
    <intent-filter>
        <action android:name="com.quickmemo.app.ACTION_ADD_TODO_FROM_NOTIFICATION" />
        <action android:name="com.quickmemo.app.ACTION_ADD_MEMO_FROM_NOTIFICATION" />
    </intent-filter>
</receiver>
```

### 5. `TodoDao.kt` — getMaxSortOrder を追加（なければ）

```kotlin
@Query("SELECT MAX(sortOrder) FROM todo_items WHERE tabId = :tabId")
suspend fun getMaxSortOrder(tabId: Int): Int?
```

### 6. `MemoEntity` の insert 用 DAO メソッド確認

`MemoDao.kt` に `@Insert` メソッドがあることを確認:

```kotlin
@Insert
suspend fun insert(memo: MemoEntity): Long
```

なければ追加。

## 動作の流れ

### ロック画面でのTodo追加:
1. ロック画面に通知が表示される（3行でTodo内容が見える）
2. 通知を下にスワイプして展開
3. 「Todo追加」ボタンをタップ
4. テキスト入力欄が通知内に表示される（Direct Reply UI）
5. テキストを入力して送信ボタンをタップ
6. `NotificationActionReceiver` が受信、DBに保存
7. 通知が自動更新され、追加したTodoが表示される
8. **ロック解除は一切不要**

### ロック画面でのメモ追加:
1. 同様に「メモ追加」ボタンからテキスト入力
2. 新規メモとしてDB保存
3. 通知が自動更新

## 検証手順

1. アプリをアンインストール→再インストール
2. Todoを数件追加してからロック
3. ロック画面の通知に3行分の内容が表示されるか確認
4. 通知展開→「Todo追加」タップ→テキスト入力→送信
5. Logcatで `QM_NOTIF_ACTION` タグを確認:
   ```
   adb logcat -s QM_NOTIF_ACTION QM_NOTIF
   ```
6. 通知が自動更新されて追加項目が見えるか確認
7. ロック解除してアプリを開き、Todoに反映されているか確認

## 注意点

- `PendingIntent.FLAG_MUTABLE` は Android 12+ で `RemoteInput` に必須。`FLAG_IMMUTABLE` だと入力テキストが null になる。
- Direct Reply は Android 7.0 (API 24) 以上で動作。minSdk 26 なので問題なし。
- `goAsync()` を使って BroadcastReceiver 内で非同期処理を安全に実行している。
- 端末の「ロック画面の通知内容を表示」設定が「すべての通知内容を表示」になっている必要がある。設定画面にこの案内を追加することを推奨。