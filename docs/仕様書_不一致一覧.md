# 仕様書不一致一覧（2026-03-06）

## 1. 対象
- 仕様書: `docs/仕様書.md`
- 実装コード: `app/src/main/**`
- 判定日: 2026-03-06

## 2. 判定サマリ
- 結論: 現在の実装は「QuickMemo（メモ/Todo/AI/OCR）」であり、`docs/仕様書.md` の「LiteMedia Player（動画/コミック中心）」とは大枠が一致していない。
- 影響: 仕様書を基準に開発を続けると、実装済み機能の方向性と矛盾が拡大する。

## 3. 不一致一覧（重大度順）

| No | 区分 | `docs/仕様書.md` の記載 | 現在実装 | 判定 |
|---|---|---|---|---|
| 1 | プロジェクト定義 | プロジェクト名: LiteMedia Player | パッケージ/アプリ実体は QuickMemo | 不一致 |
| 2 | コア機能 | 動画プレイヤー（Media3/ExoPlayer）中心 | メモ編集・Todo・OCR・翻訳・AI補助中心 | 不一致 |
| 3 | コミック機能 | コミック本棚/閲覧（CBZ/CBR等） | コミック機能モジュールなし | 不一致 |
| 4 | ナビゲーション | BottomNavigation 3タブ（プレイヤー/コミック/設定） | `HOME/EDITOR/SEARCH/SETTINGS/TRASH/TODO/PREMIUM` | 不一致 |
| 5 | モジュール構成 | `core/player/comic/lock/settings/data` | `ads/ai/billing/calendar/data/ocr/presentation/...` | 不一致 |
| 6 | データモデル | 動画/コミック/ロック設定が中心 | `MemoEntity` と `TodoItemEntity` が中心 | 不一致 |
| 7 | ロック方式 | PIN/パターン/生体、暗号保存前提 | 生体認証ユーティリティはあるがPIN/パターン構成なし | 部分不一致 |
| 8 | メモリ管理要件 | `MemoryMonitor` / `CleanupWorker` 等の明示要件 | 同名機能なし（該当ファイルなし） | 不一致 |
| 9 | 設定項目 | プレイヤー/コミック設定群中心 | メモ・AI・翻訳・OCR・Todo設定中心 | 不一致 |
| 10 | 追加要件 | 見開き自動分割・余白トリミング（コミック向け） | `PageSplitter`/`MarginTrimmer` 実装なし | 不一致 |

## 4. 根拠（実装側）
- アプリID/名称系
  - `app/build.gradle.kts`（`namespace = "com.quickmemo.app"`, `applicationId = "com.quickmemo.app"`）
  - `app/src/main/AndroidManifest.xml`（`android:name=".QuickMemoApplication"`）
- ナビゲーション定義
  - `app/src/main/java/com/quickmemo/app/presentation/navigation/QuickMemoDestinations.kt`
- 実装ディレクトリ構成
  - `app/src/main/java/com/quickmemo/app/`（`ai/`, `ocr/`, `translation/`, `todo/` 等）
- データモデル
  - `app/src/main/java/com/quickmemo/app/data/local/entity/MemoEntity.kt`
  - `app/src/main/java/com/quickmemo/app/data/local/entity/TodoItemEntity.kt`
- 仕様書で必須の存在確認（非存在）
  - `PlayerScreen.kt`, `ComicReaderScreen.kt`, `LockScreen.kt`, `MemoryMonitor.kt` は現コードベースで未検出
- 依存関係差分
  - `app/build.gradle.kts` に `media3-exoplayer` / `media3-ui` 記載なし
  - 一方で `llama`, `mlkit(text-recognition/translate)`, `billing`, `ads` など QuickMemo向け依存が存在

## 5. 影響範囲
- 設計整合性: 仕様書準拠の新規実装は既存アーキテクチャと衝突する。
- 品質管理: テスト観点・受け入れ基準が仕様書と実装で二重化する。
- 開発効率: Copilot指示の前提がズレ、不要実装や手戻りが発生しやすい。

## 6. 推奨対応（ドキュメント運用）
- 方針A: QuickMemo継続（推奨）
  - `docs/仕様書.md` を LiteMedia 用の旧資料として明示し、QuickMemo用仕様書を新規作成する。
- 方針B: LiteMediaへ転換
  - 現在実装との差分を前提に、リポジトリ構成・依存・画面を再設計する（大規模改修）。

## 7. 次アクション案
1. `docs/仕様書.md` の先頭に「現行実装対象外（アーカイブ）」注記を追加する。
2. QuickMemo向けの正規仕様書（画面/データ/非機能/受け入れ条件）を `docs/` に新規作成する。
3. CIまたはレビュー時に「仕様書との整合チェック」項目を追加する。
